apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: autoflow
spec:
  selector:
    app: payment-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  type: ClusterIP
